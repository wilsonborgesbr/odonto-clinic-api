# Profile LOCAL — roda com MongoDB Docker
# Ativar com: ./mvnw spring-boot:run -Dspring-boot.run.profiles=local
# Ou: java -jar demo.jar --spring.profiles.active=local

spring.mongodb.uri=mongodb://localhost:27017/sistema_clinica
api.security.token.secret=bokka-local-dev-secret-2026

# Seed de usuarios dev (contas ja existem via migracao do Atlas — desligado)
bokka.seed.dev-users=false
bokka.seed.wilson-password=bokka123
bokka.seed.tainah-password=tainah123

# Logs detalhados para debug local
logging.level.com.example.demo=DEBUG
